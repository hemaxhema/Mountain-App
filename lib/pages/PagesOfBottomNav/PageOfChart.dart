import 'package:flutter/cupertino.dart';

class PageOfChart extends StatelessWidget {
  const PageOfChart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Chart"));
  }
}
