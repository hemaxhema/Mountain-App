import 'package:flutter/material.dart';

class PageOfPerson extends StatelessWidget {
  const PageOfPerson({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Person"));
  }
}
